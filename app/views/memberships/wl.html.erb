<div>
  <h2>Add <%= @membership.MailingName %> to waiting list?</h2>
  <%= form_for @membership, :url => wladd_membership_path, :html => {:method => :post} do |f| %>
  <div class="form-group row">
    <div class="offset-sm-2 col-sm-10">
      <button type="submit"class="btn btn-primary">Yes</button>  | <%= link_to "No", membership_path(@membership) %>
    </div>
  </div>
  <% end %>
</div>
